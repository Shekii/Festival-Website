<?php
	session_set_cookie_params(0, '/~b5044102','homepages.shu.ac.uk', 0, 1);
	ini_set( "session.cookie_lifetime", 1200); //SETS TIMEOUT TO 20 MINUTES FOR LOGIN
	session_start();

	// session_set_cookie_params(0, '/','localhost', 0, 1);
	// ini_set( "session.cookie_lifetime", 1200); //SETS TIMEOUT TO 20 MINUTES FOR LOGIN
	// session_start();
?>

